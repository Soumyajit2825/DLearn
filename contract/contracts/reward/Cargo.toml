[package]
name = "reward-contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[features]
testutils = []

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
